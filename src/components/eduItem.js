export const eduItem = (id) => {
    let course = '';
    let university = '';
    let start = '';
    let end = '';

    return {
        id, 
        course,
        university,
        start,
        end,
    }
}